#Application Configuration
server:
  port: 8000
  error:
    include-message: always
    include-binding-errors: always

eureka:
  client:
    serviceUrl:
      defaultZone: http://root:czNjcjN0@localhost:8099/eureka
