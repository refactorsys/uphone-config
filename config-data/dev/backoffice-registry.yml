#Application Configuration
server:
  port: 8099

eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://root:3hncmCjg3KG8h89AR3BYfZ3WxR2zeMTv@${eureka.instance.hostname}:${server.port}/eureka
